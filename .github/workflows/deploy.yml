# This is a basic workflow to help you get started with Actions

name: deploy

# Controls when the workflow will run
on:
  workflow_call:
    inputs: 
      deployTarget:
        required: true
        type: string
        
  workflow_dispatch:
    inputs: 
      deployTarget:
        description: Select the environment to deploy
        required: true
        type: environment

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  deploy:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Attempt to set the environment for the job and thus trigger a github.deployment to this environment.
   # environment: ${{ inputs.deployTarget }}

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:

      - name: Run a one-line script
        run: |
          echo the deployTaget was ${{ github.event.inputs }}
          echo BUT... did we trigger a deployment to this environment ??

